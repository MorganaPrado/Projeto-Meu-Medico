@model IEnumerable<Projeto.Models.Especialidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr style="color:white; background-color: dimgrey; ">
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.EspecialidadeId }) |
                @Html.ActionLink("Details", "Details", new { id = item.EspecialidadeId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.EspecialidadeId })
            </td>
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Voltar para página de administração", "Adm", "Home")
    <p><a class="btn btn-default" href="~/Especialidades/Create">Voltar</a></p>
</div>
