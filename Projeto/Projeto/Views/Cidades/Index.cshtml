@model IEnumerable<Projeto.Models.Cidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr style="color:white; background-color: dimgrey; ">
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CidadeId }) |
                @Html.ActionLink("Details", "Details", new { id = item.CidadeId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CidadeId })
            </td>
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Voltar para página de administração", "Adm", "Home")
    <p><a class="btn btn-default" href="~/Cidades/Create">Voltar</a></p>
</div>
